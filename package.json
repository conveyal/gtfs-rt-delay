{
  "name": "gtfs-rt-delay",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "dependencies": {
    "gtfs-realtime-bindings": "^0.0.4",
    "isomorphic-fetch": "^2.2.1",
    "moment": "^2.18.1"
  },
  "scripts": {
    "deploy": "aws s3 cp out/ s3://datatools-511ny/gtfs-rt-alerts/ --acl public-read --recursive",
    "prestart": "yarn",
    "start": "node alerts.js"
  }
}
